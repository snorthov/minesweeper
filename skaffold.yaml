apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/snorthov/minesweeper:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/snorthov/minesweeper:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: minesweeper
        chartPath: charts/minesweeper
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/snorthov/minesweeper"
          image.tag: "{{.DIGEST_HEX}}"
